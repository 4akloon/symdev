[package]
name = "symbian-async"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "async/await on Symbian's active objects: a single-threaded executor on CActiveScheduler"

[lib]
crate-type = ["rlib"]

[dependencies]
symbian-core = { path = "../symbian-core" }
symbian-sys = { path = "../symbian-sys" }
