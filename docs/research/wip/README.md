# Work-in-progress notes

One file per running task, kept by the agent doing it, updated after every finding.
This is the insurance against an agent dying mid-task: facts, decisions, dead ends and
the next step live here, not only in the agent's context. When the task lands, the
durable parts move to the experiment backlog or a spec and the file is deleted.
